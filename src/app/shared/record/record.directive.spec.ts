import { RecordDirective } from './record.directive';

describe('RecordDirective', () => {
  it('should create an instance', () => {
    // const directive = new RecordDirective();
    // expect(directive).toBeTruthy();
  });
});
